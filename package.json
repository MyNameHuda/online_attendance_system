{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "comment": "REMOVED — project ini pakai Tailwind via CDN, bukan Vite. Tidak perlu npm install atau npm run dev."
}
